package org.mwatt.tutorial.initialization;

public class InitializationTestTODO {
    /*
        instance
        class
        instance

        partial initialization
            if block - in if block but not in else block
                       in else block but not in if block
            switch block - in case block but not in default block
                           in default block but not in case block
            while loop - not before the loop
                         (not updated in while discuss in each

            if (true) {
                int x = 10;
            }
     */
}
